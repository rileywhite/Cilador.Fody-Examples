This is the location where Fody will look to find assemblies containing custom Mixin
implementation types, so copy those here, perhaps in a post-build event.

See the example post-build events for MyMixinDefinitions and MyMixins projects to see how
you might do this.
